{
  "name": "oz-backend",
  "version": "1.0.0",
  "private": true,
  "description": "OrderZap v2 — Self-hosted Convex backend with PostgreSQL",
  "scripts": {
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy",
    "convex:dashboard": "npx convex dashboard"
  },
  "dependencies": {
    "convex": "^1.21.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.0",
    "typescript": "^5.7.0",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy",
    "convex:dashboard": "npx convex dashboard"
  }
}
